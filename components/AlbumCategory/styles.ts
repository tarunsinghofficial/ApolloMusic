import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        margin: 5
    },
    title: {
        color: 'white',
        fontSize: 30,
        fontWeight: 'bold',
        margin: 10
    }
})

export default styles